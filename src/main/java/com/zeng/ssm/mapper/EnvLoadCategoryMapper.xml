<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeng.ssm.dao.EnvLoadCategoryDao">
    <resultMap id="BaseResultMap" type="com.zeng.ssm.model.EnvLoadCategory">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="title" jdbcType="VARCHAR" property="title" />
    </resultMap>

    <insert id="insert" parameterType="com.zeng.ssm.model.EnvLoadCategory" useGeneratedKeys="true" keyProperty="record.id" >
        insert into ${tableName} (id, scene_data_id, `device_id`, `work_time`)
        values (#{record.id,jdbcType=INTEGER}, #{record.sceneDataId,jdbcType=INTEGER}, #{record.deviceId,jdbcType=INTEGER}, #{
        record.workTime,jdbcType=VARCHAR})
    </insert>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        SELECT * from ${tableName} where id = #{pk};
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * from ${tableName};
    </select>

    <update id="updateByPrimaryKey" parameterType="com.zeng.ssm.model.EnvLoadCategory">
        update ${tableName}
        set
          `type` = #{record.type,jdbcType=VARCHAR},
          `power` = #{record.power,jdbcType=VARCHAR},
          `title` = #{record.title,jdbcType=VARCHAR},
          `description` = #{record.description,jdbcType=VARCHAR}
        where id = #{record.id,jdbcType=INTEGER}
    </update>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
      delete from ${tableName} where id = #{pk,jdbcType=INTEGER}
    </delete>

</mapper>